package model;

// The LockableItems abstract class provides methods and elements that all lockable items in the game should have
public abstract class LockableItems implements Item {
    public abstract boolean getIsLocked();

}
